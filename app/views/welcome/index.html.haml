- content_for(:head) do
  = javascript_include_tag 'quotes'
  = javascript_include_tag 'profil/elementTransitions'

:javascript
  var quote_id = #{@sample.try(:id)}
  
.container.home
  .et-wrapper
    .et-page
      .hero-unit{class: "#{@sample.source}"}
        - if @sample
          %h1 #{@sample.description}
        - else
          %h1 :( Nothing to say.
        .review
          %i.icon-thumbs-up.et-rotate{"et-in" => "rotateSlideIn", "et-out" => "rotateSlideOut"} 
          %i.icon-thumbs-down.et-rotate{"et-in" => "rotateSlideIn", "et-out" => "rotateSlideOut"} 
    .et-page
      .hero-unit{class: "#{@sample.source}"}
        - if @sample
          %h1 #{@sample.description}
        - else
          %h1 :( Nothing to say.
        .review
          %i.icon-thumbs-up.et-rotate{"et-in" => "rotateSlideIn", "et-out" => "rotateSlideOut"} 
          %i.icon-thumbs-down.et-rotate{"et-in" => "rotateSlideIn", "et-out" => "rotateSlideOut"} 
  
  .row-fluid
    .span12
      .add
        - if @sample
          = link_to new_quote_url(source: @sample.source) do
            %i.icon-plus
              Add your own
        - elsif params[:source]
          = link_to new_quote_url(source: params[:source]) do
            %i.icon-plus
              Add your own
        - else
          = link_to new_quote_url(source: "flattery") do
            %i.icon-plus
              Add your own   

    